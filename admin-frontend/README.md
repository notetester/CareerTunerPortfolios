# CareerTuner Admin Frontend

Admin frontend workspace for feature-owned administrator screens.

The app shell can be bootstrapped as a Vite React app when admin screens move from structure to implementation.

Feature modules live under `src/features/<feature>`.
