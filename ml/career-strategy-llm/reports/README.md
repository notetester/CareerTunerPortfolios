# C career-strategy-llm reports

이 폴더는 C Career Strategy LLM 의 기존 장문 보고서 위치다. 2026-06-30 이후 긴 실험 보고서와 누적 해석 문서는 `CareerTunerAIDocs` submodule 로 이동한다.

새 기준 경로:

```text
D:/dev/CareerTuner/docs/ai-reports/areas/c-career-strategy/reports/
```

현재 이 폴더는 기존 `reports/NN` 링크와 코드 주석을 깨지 않기 위한 transitional mirror 로 유지한다. 새 장문 보고서를 계속 누적하지 말고, main repo 에는 짧은 요약과 artifact path/commit SHA 만 남긴다.

raw model output, generated request/result JSON, benchmark manifest, aggregate summary 는 다음 submodule 에 저장한다.

```text
D:/dev/CareerTuner/docs/ai-artifacts/
```
