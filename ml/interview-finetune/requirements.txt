# 면접 답변 평가 자체 모델 LoRA 파인튜닝 (D 담당)
# GPU 서버(예: RunPod A100 40GB+)에서 설치한다. CUDA 12.x 기준.
torch>=2.3
transformers>=4.45
peft>=0.13
trl>=0.11
datasets>=3.0
accelerate>=1.0
bitsandbytes>=0.44
# 서빙용 (학습과 분리 설치 권장)
# vllm>=0.6
