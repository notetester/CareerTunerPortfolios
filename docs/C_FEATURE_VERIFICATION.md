# C 영역 기능 검증 원장 (Verification Ledger)

C 영역(취업 분석·적합도·자격증 근거·자체 모델)과 인접 회색지대의 **실제 동작 확인 이력**을 한곳에 기록한다.
목적: 매 작업마다 같은 확인을 반복하지 않고, ① 검증이 안 된 것 ② 검증했지만 이후 변경이 쌓여
재검증이 필요한 것을 즉시 식별한다.

**갱신 규칙**: 해당 기능을 바꾸는 PR을 머지하면 그 행의 '최종 검증'을 갱신하거나 상태를 `재검증 필요`로
내린다. 라이브 확인은 배포된 dev 환경(EC2) 기준. 상태 의미 — ✅ 검증됨(해당 시점 기준 현행) /
🔶 부분(일부 경로만 검증) / ⏳ 재검증 필요(검증 후 변경 누적) / ❌ 미구현(요구는 있으나 기능 없음).

## 1. 적합도 분석 코어 (뉴로-심볼릭)

| 기능 | 상태 | 구현 기준 | 최종 검증 | 비고 |
| --- | --- | --- | --- | --- |
| 규칙엔진 판단값(fitScore/matched/missing/applyDecision) | ✅ | 코어 | 단위테스트 상시 + E2E 60케이스(2026-07-02, AIDocs 83) | 판단값은 LLM 미관여(불변식) |
| OSS 3B 설명 생성 + E1 grounding guard | ✅ | OSS 연동 | E2E production 경로 60케이스: 무방비 노출 0(2026-07-02) | 재시도→폴백 흡수 실증 |
| R3 evidence gate(보유단정 검출→REVIEW_REQUIRED) | ✅ | PR #175 | 같은 E2E + replay 테스트(AIDocs 82) | E1과 상보성 실증 |
| 스킬 별칭 false-negative(JS↔JavaScript류) | ✅ | (이번 PR) | SkillAliasCatalog(동의어 큐레이션) 도입, 단위테스트(약어/표기차 매칭·false-positive 미발생) 통과 | **가산적**(substring OR 정규형 일치) — 매칭만 늘고 안 줄임. 상하위(MSSQL↔SQL)는 제외(오귀속 방지). 규칙엔진 단일소스라 OSS·호스티드 전파 |
| 기업맥락(B company_analysis) 주입 | ✅ | 코어 | conflation 실측 0/8, 이중판정 일치(2026-07-10, PR #347 기록) | 판단값 미관여·전략 참고용만 |
| analysis_confidence 정량화 | ✅ | 코어 | 단위테스트 | **결정론 감점식**(입력 결손 기반 0~100). company_analysis 부재는 미반영(판단값 무영향이라 설계상 제외 — 사유 표기 개선 여지) |
| company_analysis 부재 시 degrade | ✅ | 코어 | 코드 확인(2026-07-11): 기업맥락 null로 공고 기반 진행, 분석 비중단 | 실패 '표시'는 없음(무맥락 진행) |

## 2. 자격증 근거 시스템

| 기능 | 상태 | 구현 기준 | 최종 검증 | 비고 |
| --- | --- | --- | --- | --- |
| cert-need-gate(적재적소 추천 — 5신호, 기본 OFF) | ✅ | 커밋 06ff8636~ | 단위테스트 + 탭 USER_REQUESTED 배선(2f) + **라이브 발화 확인**(2026-07-11: 공고 자격증 명시 케이스에서 REQUIRED_OR_STRONGLY_PREFERRED + 신호 3개 정확) | 탭 요청도 '평가'(불필요면 후순위 강등) |
| 국가자격 종류판별(T/S) 오프라인 스냅샷 | ✅ | PR #353 | 무결성 게이트 테스트(CP949 재현 포함) + CI + 배포(2026-07-10) | Q-Net 장애 무관 |
| 국가기술 시험일정 — 통합 API(jmCd 512종) | ✅ | PR #364, 키 배선 PR #365 | **라이브 E2E 완료(2026-07-11, #365 배포 후)**: 재분석→VERIFIED_CURRENT+실제 2026 회차(제3회 필기 8/7 등, 오프라인 수확분과 일치) 응답 확인. 그 전에 EC2 키 미주입 갭을 같은 E2E 로 발견해 Secrets 배선 | 컴퓨터시스템기사 1종 레거시 경로(canonical 미확정) |
| 민간자격 등록 조회(odcloud) | ✅ | Increment 2b | 라이브 검증(2026-07 초) + 단위테스트 + 라이브 E2E(2026-07-11) | 일정은 항상 MANUAL_REQUIRED. 약어(SQLD 등)는 검증된 별칭 카탈로그로 공식 등록명 재조회(이번 PR — SQLD→SQL 라이브 확인) |
| 국가전문자격 일정 자동확인(PREANNOUNCED 번들) | ✅ | (이번 PR) | 단위테스트(실번들 로드·그룹 공유·연도 가드) + 검색 API 라이브(공인노무사 scheduleQueryable=true, 2026-07-11 로컬) | 사전공고(안) 신뢰층 — '최종 공고 확인 필요' 문구 강제. 연도 지나면 자동 비활성 |
| 근거 snapshot 생성·저장·읽기 E2E | ✅ | Increment 2e | 생성→fit_analysis.certificate_evidence→UI 배지 검증 + 트랜잭션 밖 I/O 교정 | 읽기 경로 provider 무호출 |
| 사용자 대면 자격증 검색(/certificates) | ✅ | (이번 PR) | 라이브 검증(2026-07-11 로컬): SQLD→SQL 별칭 해석·정보처리 국가 2종·공인노무사 전문자격 | 검색=조회(추천 아님) 문구 명시. 별칭은 odcloud 실측 검증분만 |
| 구형 getJMList 폴백 | ⏳ | PR #364 | Q-Net 원서버 장애 지속(2026-07-12 재확인 resultCode 99) — 외부 의존이라 라이브 검증 불가 | 통합 API가 주경로라 기능 영향 없음. 복구 시 resultCode=00 확인 |

## 3. 취업 분석(경향)·장기 전략·플래너

| 기능 | 상태 | 구현 기준 | 최종 검증 | 비고 |
| --- | --- | --- | --- | --- |
| 취업 분석 5탭(지원 경향/반복 부족역량/직무별 준비도/점수 변화/추천 방향) | ✅ | AnalysisServiceImpl(결정론 집계) | 단위테스트 + **라이브 API 검증(2026-07-12)**: 시드 12건에서 반복 부족역량(Java/Kafka/K8s/Redis 각 2회)·직무별 준비도(백엔드 3건 avg 46 등)·점수 변화·추천 방향 정상 집계 | A+B 산출물을 fit 단위 비교 후 집계 |
| 장기 자격증 전략(희망직무 기준 결정론) | ✅ | Increment 2g | 단위테스트 + 학습탭 카드 확인(2026-07-08경) | 외부 API 무호출 |
| 커리어 목표·학습계획 CRUD(CareerPlan) | ✅ | 기존 | 회귀 스위트 | 수기 입력형(사용자 목표·계획 관리). **자동 생성은 장기 로드맵(/career-roadmap)이 담당** — 역할 분리 확정 |
| **장기 커리어 로드맵(/career-roadmap)** | ✅ | (이번 PR) | 라이브 검증(2026-07-11 로컬): 지원마감 7건+정보처리기사 실회차+학습블록 3개 배치, 전체 플래너 반영→월 달력 표시까지 관통. 회차 중복(접수변형) dedup 수정 포함 | 결정론(LLM 미관여). 실일정 vs 계획블록(planningBlock) 계약 구분. 학습블록=최근 fit missing 빈도 상위 3 |
| 플래너(일정/메모/오버레이 + **월/연 달력 뷰**) | ✅ | 기존 + 이번 PR | 라이브 검증(2026-07-11 로컬): 월 그리드에 시드 일정 칩 정확 배치, 연 뷰 12개월 카드·건수, 뷰별 기간 재조회 | 외부(구글) 캘린더 연동은 여전히 ❌ |
| 전략초안→플래너 일정 생성(fit 연동) | ✅ | 기존 | 시드+화면 확인(2026-07-10) | 수동 일정엔 지원건 피커 없음(전략초안 경로만 연결) |
| 자격증 시험일정→플래너 연동 | ✅ | (이번 PR) | 라이브 검증(2026-07-11 로컬): 근거 카드 버튼 1클릭 → 접수 DEADLINE+필기/실기 EVENT 3건 POST 200 | VERIFIED/PREANNOUNCED 상태에서만 노출(미확인 날짜로 일정 생성 금지) |
| 외부 캘린더 연동(iCal .ics 내보내기) | ✅ | (이번 PR) | PlannerICalService + GET /planner/export.ics, 단위테스트(종일/시각 이벤트·KST→UTC·RFC5545 이스케이프) + 라이브(유효 VCALENDAR 생성) | 단방향 내보내기(구글/애플/아웃룩 '가져오기') — OAuth·외부호출 없음. 양방향 동기화는 별도 과제 |
| A영역 AI 분석 산출물 영속 + 사용자 조회 + C 주입 | ✅ | (이번 PR) | **신규 profile_ai_analysis 테이블**(feature별 최신 upsert) + 저장/조회 + 프론트 시드 + C composeProfileInsight(PROFILE_SUMMARY 요약/강점/보완점→프롬프트 참고섹션). 라이브: /profile/ai-analysis 저장분 조회(요약·강점·약점·criteria)·재분석 관통(fitScore 불변). dev DB 테이블+시드 적용 | **A 산출물이 이제 새로고침 후에도 보이고 C 입력창구로 흐름.** 판단값 무관(프롬프트 참고용), OSS 미주입(학습정합)·호스티드만, profileInsight=null이면 프롬프트 byte 동일. A 강점의 conflation은 E1 가드 영역 |

## 4. 회색지대·데모·인프라

| 기능 | 상태 | 구현 기준 | 최종 검증 | 비고 |
| --- | --- | --- | --- | --- |
| 회색지대 통합 QA(딥링크·다크모드·반응형·3플랫폼) | ✅ | PR #342/#344/#345 | 52건 중 51 수정, 전부 배포+라이브 번들 검증(2026-07-09~10) | 이후 타 영역 머지 다수 — 대규모 UI 변경 시 ⏳ 전환 |
| 데모 시드(92만번대 멱등) | ✅ | PR #347 | DB 카운트·JSON_VALID 전량 검증 + 적용(2026-07-10) | 재실행 안전 |
| 공개 mock 데모 | ✅ | (이전 라운드) | 로그인 복구+기능 시드 검증 | 백로그 5건 별도 |
| 시험일정·jmCd·민간자격 오프라인 데이터 | ✅ | PR #357/#362 | 전량 수확 + totalCount 일치 + 앵커 검증, ai-artifacts 보관(2026-07-11) | 2027년 일정은 게시 후 재수확 |

## 5. 재검증 트리거 요약 (지금 해야 하는 것)

1. ~~PR #364 배포 후 E2E~~ → **완료(2026-07-11, PR #365 배포 후 라이브 관통 확인)**.
2. 취업 분석 화면을 시드 대량 데이터로 확인(집계 5탭이 시연 데이터에서 자연스러운지).
3. Q-Net 원서버 복구 시: 구형 폴백 경로 + 국가전문자격 일정 API 라이브 검증.
4. ~~민간자격 약어 별칭 정규화~~ → **완료(이번 PR — 실측 검증 별칭만 채택: SQLD/SQLP→SQL, ADsP/ADP→데이터분석, CPPG 등)**.
5. 이번 PR 기능들의 배포 dev 재검증(로컬 검증 완료분): 로드맵·달력·검색을 EC2 배포 후 스팟 확인.
