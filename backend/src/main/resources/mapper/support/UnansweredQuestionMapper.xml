<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careertuner.support.chatbot.UnansweredQuestionMapper">

    <!-- 같은 질문 반복도 행을 계속 append (조회에서 의미 군집화로 묶음). source/status 는 DB 기본값. -->
    <insert id="insert">
        INSERT INTO chatbot_unanswered_question
            (question, question_norm, top_similarity, embedding, best_faq_id, user_id, conversation_id)
        VALUES
            (#{question}, #{questionNorm}, #{topSimilarity}, #{embedding}, #{bestFaqId}, #{userId}, #{conversationId})
    </insert>

</mapper>
