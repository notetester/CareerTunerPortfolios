<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careertuner.support.chatbot.quota.ChatbotQuotaPolicyMapper">

    <select id="findPolicy" resultType="com.careertuner.support.chatbot.quota.ChatbotQuotaPolicy">
        SELECT id, enabled, daily_limit AS dailyLimit,
               updated_by AS updatedBy, created_at AS createdAt, updated_at AS updatedAt
          FROM chatbot_quota_policy
         WHERE id = 1
    </select>

    <update id="updatePolicy">
        UPDATE chatbot_quota_policy
           SET enabled = #{enabled},
               daily_limit = #{dailyLimit},
               updated_by = #{updatedBy}
         WHERE id = 1
    </update>

</mapper>
