<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.careertuner.applicationcase.mapper.ApplicationCaseMapper">

    <resultMap id="ApplicationCaseResultMap" type="com.careertuner.applicationcase.domain.ApplicationCase">
        <id property="id" column="id"/>
        <result property="favorite" column="is_favorite"/>
        <result property="archivedAt" column="archived_at"/>
        <result property="deletedAt" column="deleted_at"/>
    </resultMap>

    <insert id="insertApplicationCase" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO application_case
            (user_id, company_name, job_title, posting_date, source_type, status, is_favorite)
        VALUES
            (#{userId}, #{companyName}, #{jobTitle}, #{postingDate}, #{sourceType}, #{status}, #{favorite})
    </insert>

    <select id="findApplicationCasesByUserId" resultMap="ApplicationCaseResultMap">
        SELECT *
        FROM application_case
        WHERE user_id = #{userId}
          AND deleted_at IS NULL
        <if test="!includeArchived">
          AND archived_at IS NULL
        </if>
        ORDER BY is_favorite DESC, updated_at DESC, id DESC
    </select>

    <select id="findApplicationCaseByIdAndUserId" resultMap="ApplicationCaseResultMap">
        SELECT *
        FROM application_case
        WHERE id = #{id} AND user_id = #{userId} AND deleted_at IS NULL
    </select>

    <update id="updateApplicationCase">
        UPDATE application_case
        SET company_name = #{companyName},
            job_title = #{jobTitle},
            posting_date = #{postingDate},
            source_type = #{sourceType},
            status = #{status},
            is_favorite = #{favorite},
            archived_at = #{archivedAt}
        WHERE id = #{id} AND user_id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="markAnalysisCompleted">
        UPDATE application_case
        SET status = CASE
                WHEN status IN ('DRAFT', 'ANALYZING') THEN 'READY'
                ELSE status
            END
        WHERE id = #{id} AND user_id = #{userId}
    </update>

    <delete id="deleteApplicationCase">
        DELETE FROM application_case
        WHERE id = #{id} AND user_id = #{userId}
    </delete>

    <update id="softDeleteApplicationCase">
        UPDATE application_case
        SET deleted_at = CURRENT_TIMESTAMP
        WHERE id = #{id} AND user_id = #{userId} AND deleted_at IS NULL
    </update>

    <insert id="insertStatusHistory">
        INSERT INTO application_case_status_history
            (application_case_id, changed_by_user_id, previous_status, new_status, memo)
        VALUES
            (#{applicationCaseId}, #{changedByUserId}, #{previousStatus}, #{newStatus}, #{memo})
    </insert>

    <delete id="deleteFitAnalysesByCaseId">
        DELETE FROM fit_analysis
        WHERE application_case_id = #{applicationCaseId}
    </delete>

    <insert id="insertFitAnalysis" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO fit_analysis
            (application_case_id, fit_score, matched_skills, missing_skills,
             recommended_study, recommended_certificates, strategy)
        VALUES
            (#{applicationCaseId}, #{fitScore}, #{matchedSkills}, #{missingSkills},
             #{recommendedStudy}, #{recommendedCertificates}, #{strategy})
    </insert>

    <select id="findLatestFitAnalysisByCaseId" resultType="com.careertuner.applicationcase.domain.FitAnalysis">
        SELECT *
        FROM fit_analysis
        WHERE application_case_id = #{applicationCaseId}
        ORDER BY id DESC
        LIMIT 1
    </select>

    <insert id="insertAiUsageLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_usage_log
            (user_id, application_case_id, feature_type, status, model,
             input_tokens, output_tokens, token_usage, credit_used, error_message)
        VALUES
            (#{userId}, #{applicationCaseId}, #{featureType}, #{status}, #{model},
             #{inputTokens}, #{outputTokens}, #{tokenUsage}, #{creditUsed}, #{errorMessage})
    </insert>
</mapper>
