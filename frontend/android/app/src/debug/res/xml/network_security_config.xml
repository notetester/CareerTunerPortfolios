<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug source-set only; the sync policy still limits HTTP server.url to local/private hosts. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
